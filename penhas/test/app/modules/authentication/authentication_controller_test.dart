import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:penhas/app/modules/authentication/authentication_module.dart';

void main() {
  initModule(AuthenticationModule());
  // AuthenticationController authentication;
  //
  setUp(() {
    //     authentication = AuthenticationModule.to.get<AuthenticationController>();
  });

  group('AuthenticationController Test', () {
    //   test("First Test", () {
    //     expect(authentication, isInstanceOf<AuthenticationController>());
    //   });

    //   test("Set Value", () {
    //     expect(authentication.value, equals(0));
    //     authentication.increment();
    //     expect(authentication.value, equals(1));
    //   });
  });
}
