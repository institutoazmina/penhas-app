import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:penhas/app/modules/authentication/registration/registration_page.dart';

main() {
  testWidgets('RegistrationPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegistrationPage(title: 'Registration')));
    //  final titleFinder = find.text('Registration');
    //  expect(titleFinder, findsOneWidget);
  });
}
