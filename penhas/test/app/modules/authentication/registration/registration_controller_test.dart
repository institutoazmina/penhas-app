import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:penhas/app/modules/authentication/registration/registration_controller.dart';
import 'package:penhas/app/modules/authentication/registration/registration_module.dart';

void main() {
  initModule(RegistrationModule());
  // RegistrationController registration;
  //
  setUp(() {
    //     registration = RegistrationModule.to.get<RegistrationController>();
  });

  group('RegistrationController Test', () {
    //   test("First Test", () {
    //     expect(registration, isInstanceOf<RegistrationController>());
    //   });

    //   test("Set Value", () {
    //     expect(registration.value, equals(0));
    //     registration.increment();
    //     expect(registration.value, equals(1));
    //   });
  });
}
