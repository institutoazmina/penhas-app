import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:penhas/app/modules/authentication/password_recovery/password_recovery_page.dart';

main() {
  testWidgets('PasswordRecoveryPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PasswordRecoveryPage(title: 'PasswordRecovery')));
    //  final titleFinder = find.text('PasswordRecovery');
    //  expect(titleFinder, findsOneWidget);
  });
}
