import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:penhas/app/modules/authentication/password_recovery/password_recovery_controller.dart';
import 'package:penhas/app/modules/authentication/password_recovery/password_recovery_module.dart';

void main() {
  initModule(PasswordRecoveryModule());
  // PasswordRecoveryController passwordrecovery;
  //
  setUp(() {
    //     passwordrecovery = PasswordRecoveryModule.to.get<PasswordRecoveryController>();
  });

  group('PasswordRecoveryController Test', () {
    //   test("First Test", () {
    //     expect(passwordrecovery, isInstanceOf<PasswordRecoveryController>());
    //   });

    //   test("Set Value", () {
    //     expect(passwordrecovery.value, equals(0));
    //     passwordrecovery.increment();
    //     expect(passwordrecovery.value, equals(1));
    //   });
  });
}
