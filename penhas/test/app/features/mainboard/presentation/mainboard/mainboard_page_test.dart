import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:penhas/app/features/mainboard/presentation/mainboard/mainboard_page.dart';

main() {
  testWidgets('MainboardPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MainboardPage(title: 'Mainboard')));
    //  final titleFinder = find.text('Mainboard');
    //  expect(titleFinder, findsOneWidget);
  });
}
