import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:penhas/app/features/authentication/presentation/reset_password/reset_password_controller.dart';
import 'package:penhas/app/features/authentication/presentation/reset_password/reset_password_module.dart';

void main() {
  initModule(ResetPasswordModule());
  // ResetPasswordController resetpassword;
  //
  setUp(() {
    //     resetpassword = ResetPasswordModule.to.get<ResetPasswordController>();
  });

  group('ResetPasswordController Test', () {
    //   test("First Test", () {
    //     expect(resetpassword, isInstanceOf<ResetPasswordController>());
    //   });

    //   test("Set Value", () {
    //     expect(resetpassword.value, equals(0));
    //     resetpassword.increment();
    //     expect(resetpassword.value, equals(1));
    //   });
  });
}
