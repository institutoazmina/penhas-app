import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:penhas/app/features/authentication/presentation/reset_password_two/reset_password_two_page.dart';

main() {
  testWidgets('ResetPasswordTwoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ResetPasswordTwoPage(title: 'ResetPasswordTwo')));
    //  final titleFinder = find.text('ResetPasswordTwo');
    //  expect(titleFinder, findsOneWidget);
  });
}
