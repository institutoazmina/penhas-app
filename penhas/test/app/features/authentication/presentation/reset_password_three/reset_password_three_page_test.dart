import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:penhas/app/features/authentication/presentation/reset_password_three/reset_password_three_page.dart';

main() {
  testWidgets('ResetPasswordThreePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ResetPasswordThreePage(title: 'ResetPasswordThree')));
    //  final titleFinder = find.text('ResetPasswordThree');
    //  expect(titleFinder, findsOneWidget);
  });
}
